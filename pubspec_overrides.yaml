dependency_overrides:
# where are we? flutter/utils/flutterfire/<here>
  abstractions:
    path: ../../perception/abstractions
  firebase_auth_service_interface:
    path: ../../../../dart/interfaces/firebase_auth_service_interface

  # enspyr_lints:
  #   path: 
  # types_for_auth:
  #   path: 
  # json_utils:
  #   path: 